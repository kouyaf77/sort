#include<iostream>
#include<cstdlib>
#include<stack>

int main() {
    stack<int> S;
    int a, b, x;
    string s;

    while( cin >> s) {
        if (s[0] == '+') {
            a = S.top(); S.
        }
    }
    return 0;
}